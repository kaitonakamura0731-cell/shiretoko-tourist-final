# AGENTS.md

このファイルは**ポインタ**です。内容を複製すると必ず片方だけ更新されて食い違うため、
実体はここに書きません。

## まず読むもの

- **`.claude/CLAUDE.md`** — このプロジェクトの正本(SSOT)。作業方針・制約・技術スタック・引き継ぎ状況はすべてそこにある


## Claude Code から委譲された作業の約束事

Claude Code が司令塔、Codex が実装・量産を担当する二段構成で運用している。
渡されたブリーフに加えて、次を常に守る。

- ブリーフの「対象ファイル」allowlist の外を書き換えない
- **`git add` / `commit` / `push` / `reset` / `checkout` を実行しない**。
  検収は Claude 側が基準SHAからの差分で行うため、コミットされると検収が成立しなくなる
- 頼まれた範囲だけ変更する。ついでのリファクタ・依存追加・フォーマット一括変更はしない
- 完了報告では、実際に検証したコマンドとその出力を根拠として示す

## 秘匿ガード

成果物・コミットメッセージ・報告に次を書かない:
原価 / 利益率 / 差益 / 決済手数料の内訳 / Pay.jp の名前。
カード手数料に触れる場合は「2.5%」とだけ書く。
